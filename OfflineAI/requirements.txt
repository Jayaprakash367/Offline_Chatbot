# OfflineAI — Python Dependencies
# All libraries work OFFLINE after installation.
# No cloud APIs, no telemetry, no internet-dependent packages.

pyttsx3>=2.90          # Offline text-to-speech (Windows SAPI5)
vosk>=0.3.45           # Offline speech-to-text engine (high accuracy)
pyaudio>=0.2.13        # Microphone access
psutil>=5.9.0          # System status (CPU, RAM, battery)
SpeechRecognition>=3.10.0  # Speech recognition for Tamil (requires internet)
requests>=2.31.0       # HTTP client for OpenAI/Ollama routing
fastapi>=0.115.0       # Jarvis web API backend
uvicorn>=0.30.0        # ASGI server for FastAPI
pywebview>=5.1         # Desktop window wrapper for Jarvis web UI
numpy>=1.26.0          # Vision frame decoding
opencv-python>=4.10.0  # Camera face detection (vision emotion pipeline)
