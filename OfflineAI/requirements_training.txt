# Optional dependencies for billion-parameter Jarvis fine-tuning (QLoRA profile)
# Install only when running training scripts.

transformers>=4.44.0
peft>=0.12.0
accelerate>=0.34.0
datasets>=2.21.0
bitsandbytes>=0.43.0
torch>=2.3.0
sentencepiece>=0.2.0
